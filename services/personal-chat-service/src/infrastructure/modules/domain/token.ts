import { moduleProviderToken } from "../../utils/provider-token";

export const DomainModuleName = "Domain";

export const DOMAIN = moduleProviderToken(DomainModuleName, "domain");

export const DOMAIN_NAME = moduleProviderToken(DomainModuleName, "domain_name");
